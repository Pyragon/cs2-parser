const A = require('arcsecond');

const up = require('./util-parsers');
const v = require('./variables');

const ifStatement = A.coroutine(function* () {
});

module.exports = {
	ifStatement,
	whileStatement
};
